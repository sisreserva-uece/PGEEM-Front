/**
 * A placeholder component to satisfy the MainDataViewComponent prop
 * for MasterDetailSheet when it's used in an edit-only context.
 * It renders nothing.
 */
export function TipoEquipamentoMainDataView() {
  return null;
}
